#include<stdio.h>

int main()
{
	int x = 5, y=6;

	int charcount = 0;
	charcount = printf("\n Value of x = %d and y = %d",x,y); // Return value of a printf Statement.
	printf("\n Total number of characters printed in Console = %d",charcount);
}
